<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Стиль для светлой темы -->
    <style name="Theme.MyApplication" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/background_color</item>
    </style>

    <!-- Стиль для ночной темы -->
    <style name="Theme.MyApplication.Night" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/background_color_dark</item>
    </style>

    <!-- Стиль для экрана Splash -->
    <style name="Theme.MyApplication.SplashTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_screen</item>
    </style>

</resources>
